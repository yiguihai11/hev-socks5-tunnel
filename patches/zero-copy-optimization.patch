--- a/src/hev-session-manager.c
+++ b/src/hev-session-manager.c
@@ -42,6 +42,7 @@
 #include "hev-config.h"
 #include "hev-logger.h"
 #include "hev-memory-pool.h"
+#include "hev-session-manager-patch.h"
 #include "hev-task.h"
 #include "hev-task-io.h"
 #include "hev-socks5.h"
@@ -569,6 +570,13 @@ sniff_http_host (HevSocks5SessionTCP *self, char *hostname_buffer, size_t buffe
 {
     struct pbuf *p;
     size_t total_len = 0;
+
+    /* 使用零拷贝优化版本（如果启用） */
+    if (hev_session_manager_is_protocol_zerocopy_enabled()) {
+        LOG_D("%p session: Using zero-copy HTTP host parsing", self);
+        return sniff_http_host_zerocopy(self, hostname_buffer, buffer_len);
+    }

     unsigned char buffer[1024];
     char *request_start;
@@ -657,6 +665,13 @@ sniff_client_hello (HevSocks5SessionTCP *self, HevTLSClientHello *hello)
     unsigned char *ptr = NULL;
     size_t total_len = 0;

+    /* 使用零拷贝优化版本（如果启用） */
+    if (hev_session_manager_is_protocol_zerocopy_enabled()) {
+        LOG_D("%p session: Using zero-copy TLS SNI parsing", self);
+        char hostname_buffer[256];
+        return sniff_tls_sni_zerocopy(self, hostname_buffer, sizeof(hostname_buffer));
+    }
+
     /* Copy data from queue for analysis */
     for (p = self->queue; p && total_len < (sizeof (buffer) - 1); p = p->next) {
         size_t copy_len = p->len;
--- a/src/hev-main.c
+++ b/src/hev-main.c
@@ -28,6 +28,7 @@
 #include "hev-config.h"
 #include "hev-logger.h"
 #include "hev-main.h"
+#include "hev-session-manager-patch.h"
 #include "hev-socks5-tunnel.h"
 #include "hev-task.h"
 #include "hev-task-system.h"
@@ -42,6 +43,11 @@ main (int argc, char *argv[])
     if (hev_config_init (argc, argv) < 0)
         return -1;

+    /* 初始化零拷贝优化 */
+    if (hev_session_manager_enable_protocol_zerocopy() < 0) {
+        LOG_W ("Failed to enable protocol zero-copy optimization");
+    }
+
     hev_logger_init (hev_config_get_log_level ());

     hev_task_system_init ();

--- a/src/Makefile
+++ b/src/Makefile
@@ -35,6 +35,7 @@ SOURCES = \
 	hev-tunnel-linux.c \
 	hev-tunnel-macos.c \
 	hev-tunnel-windows.c \
+	hev-session-manager-patch.c \
 	hev-task.c \
 	hev-socks5.c \
 	hev-socks5-tunnel.c
@@ -85,6 +86,9 @@ endif

 CFLAGS += -Wall -O2

+# 零拷贝优化编译选项
+CFLAGS += -DENABLE_PROTOCOL_ZERO_COPY=1
+
 LDLIBS += -lpthread

 ifeq ($(STATIC),1)